language: node_js
node_js:
  - "lts/*"
cache:
  directories:
    - "packages/react-jsx-highcharts/node_modules"
    - "packages/react-jsx-highstock/node_modules"
    - "packages/react-jsx-highmaps/node_modules"
stages:
  - install
  - name: prebuild
    if: branch != master
  - test
env:
  - PKG_DIR=packages/react-jsx-highcharts
  - PKG_DIR=packages/react-jsx-highstock
  - PKG_DIR=packages/react-jsx-highmaps
before_install:
  - cd $PKG_DIR
jobs:
  include:
    - stage: before_script
      name: prebuild
      script: cd ../react-jsx-highcharts && npm run build:prod && cd -
script:
  - npm run lint
  - npm run test:${TEST_SUITE}
  - npm run build:prod
after_success:
  - bash <(curl -s https://codecov.io/bash) || echo "Codecov did not collect coverage reports"
